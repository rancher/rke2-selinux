on:
  push:
    branches:
      - master
      - gha_migration
  pull_request:

name: Build EL7
permissions:
    contents: read
jobs:
  build-rpm-el7:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: build-rpm
      run: |
        make centos7-build
        